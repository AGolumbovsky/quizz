// Progress questions in CurrentQuestion (need to be independent from the CurrentStep(all current questions are inside step One))
// Add NextQuestion and PrevQuestion buttons

export const EndOfQuiz = () => {
	return;
	<div>End of Quiz</div>;
};
